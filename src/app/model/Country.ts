export class Country {
  name: string;
  dial_code: string;
  code: string;

  constructor(name: string, dial_code: string, code: string) {
    this.name = name;
    this.dial_code = dial_code;
    this.code = code;
  }
}
